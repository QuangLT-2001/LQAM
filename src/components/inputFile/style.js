import Styled from "styled-components"
export const InputFileWrapper = Styled.label`
     & > svg {
          cursor: pointer;
          font-size: 1.5rem;
     }
     input[type="file"] {
          display: none;
     }

`